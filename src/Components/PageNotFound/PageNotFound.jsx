import React from 'react';

const PageNotFound = (props) => {
    return (
        <div style={{...props.contStyle}}>
            page Not Found :(
        </div>
    );
}

export default PageNotFound;
